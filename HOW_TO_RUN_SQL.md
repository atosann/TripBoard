# 🎯 schema.sqlを実行する具体的な手順（完全版）

## 📂 準備：schema.sqlファイルを開く

### ステップ1: ZIPファイルを解凍
1. ダウンロードした `trip-board.zip` をダブルクリック
2. 解凍されたフォルダが表示される

### ステップ2: schema.sqlを開く
```
trip-board フォルダを開く
  ↓
supabase フォルダを開く
  ↓
schema.sql ファイルをダブルクリック
  ↓
メモ帳 または テキストエディタで開く
```

### ステップ3: 全ての内容をコピー
1. **Ctrl+A**（Mac: Cmd+A）を押して全て選択
2. **Ctrl+C**（Mac: Cmd+C）を押してコピー

💡 これで準備完了！次はSupabaseに移動します。

---

## 🌐 Supabase SQL Editorで実行

### ステップ1: Supabaseを開く

1. ブラウザで https://supabase.com を開く
2. 「Sign in」でログイン
3. あなたのプロジェクトをクリック（例：trip-board）

### ステップ2: SQL Editorを開く

画面の左側にメニューがあります：
```
┌─────────────────┐
│ 🏠 Home         │
│ 📊 Table Editor │
│ 💾 SQL Editor   │ ← ★ここをクリック★
│ 🗄️  Database    │
│ 🔐 Auth         │
│ 📁 Storage      │
└─────────────────┘
```

**「SQL Editor」をクリック**してください！

### ステップ3: 新しいクエリを作成

SQL Editor画面が開いたら：

```
┌──────────────────────────────────────┐
│  SQL Editor          [+ New query]   │ ← ★ここをクリック★
├──────────────────────────────────────┤
│                                      │
│  (空の画面)                           │
│                                      │
└──────────────────────────────────────┘
```

**右上の「+ New query」ボタンをクリック**

### ステップ4: SQLをペースト（貼り付け）

エディタ画面が開きます：

```
┌──────────────────────────────────────┐
│  Untitled Query                      │
├──────────────────────────────────────┤
│  1 |                                 │ ← ★ここにカーソルを置く★
│  2 |                                 │
│  3 |                                 │
│    |                                 │
│    ↓                                 │
│   ここに貼り付ける！                   │
│                                      │
│                       [RUN ▶]        │
└──────────────────────────────────────┘
```

1. エディタ内（行番号の右側）をクリック
2. **Ctrl+V**（Mac: Cmd+V）を押して貼り付け
3. schema.sqlの内容が全て表示される

### ステップ5: 実行する

```
┌──────────────────────────────────────┐
│  -- Trip Board データベーススキーマ    │
│  CREATE EXTENSION IF NOT EXISTS...   │
│  CREATE TABLE public.users (         │
│    id UUID PRIMARY KEY...            │
│  );                                  │
│  ...                                 │
│  (たくさんのSQL文が表示される)         │
│                                      │
│                       [RUN ▶]        │ ← ★ここをクリック★
└──────────────────────────────────────┘
```

**右下の「RUN」ボタン（▶マーク）をクリック**

### ステップ6: 実行完了を待つ

```
┌──────────────────────────────────────┐
│  Running query...  ⏳                │
│                                      │
│  (10-30秒ほど待ちます)                 │
└──────────────────────────────────────┘
```

### ステップ7: 成功を確認

成功すると画面下部に表示されます：

```
┌──────────────────────────────────────┐
│  ✅ Success. No rows returned         │
│  Execution time: 1.234s              │
└──────────────────────────────────────┘
```

または

```
┌──────────────────────────────────────┐
│  ✅ Rows: 7                           │
│  (カテゴリデータが挿入された)           │
└──────────────────────────────────────┘
```

**緑色のチェックマーク ✅ が出れば成功です！**

---

## 🔍 テーブルが作成されたか確認

### 左メニューの「Table Editor」をクリック

```
┌─────────────────┐
│ 🏠 Home         │
│ 📊 Table Editor │ ← ★ここをクリック★
│ 💾 SQL Editor   │
│ 🗄️  Database    │
└─────────────────┘
```

### 以下のテーブルが表示されればOK！

```
┌──────────────────────────────────────┐
│  Tables:                             │
│  ✓ activity_logs                     │
│  ✓ categories          (7 rows)      │
│  ✓ messages                          │
│  ✓ participants                      │
│  ✓ posts                             │
│  ✓ reports                           │
│  ✓ users                             │
└──────────────────────────────────────┘
```

**7つのテーブルがあればバッチリ成功です！** 🎉

---

## ❓ よくある質問

### Q1: 「RUN」ボタンが見つからない
**A**: 画面を下にスクロールしてください。右下にあります。

### Q2: エラーが出た「relation "xxx" already exists」
**A**: 既に実行済みです。無視してOK！次のステップに進んでください。

### Q3: 真っ白な画面になった
**A**: 「+ New query」を押し忘れています。もう一度押してください。

### Q4: コピーした内容が途中で切れている
**A**: schema.sqlファイルで必ず **Ctrl+A（全選択）→ Ctrl+C（コピー）** してください。

### Q5: 「permission denied」エラー
**A**: プロジェクトの所有者アカウントでログインしているか確認してください。

---

## 📝 実際の操作まとめ（超シンプル版）

```
① trip-board.zip を解凍
② supabase/schema.sql を開く
③ Ctrl+A → Ctrl+C でコピー
④ https://supabase.com にログイン
⑤ プロジェクトを選択
⑥ 左メニュー「SQL Editor」をクリック
⑦ 「+ New query」をクリック
⑧ Ctrl+V で貼り付け
⑨ 「RUN」をクリック
⑩ ✅ が出たら成功！
```

**これだけです！** 

たった10ステップでデータベースが完成します！🚀

---

## 🎬 動画で見たい方へ

もし動画での説明が必要な場合は、以下のような操作になります：

1. Supabaseにログイン
2. SQL Editorを開く
3. コードを貼り付け
4. Runボタンを押す

（実際の画面で試してみるのが一番わかりやすいです！）

---

## ✅ 完了チェックリスト

- [ ] schema.sqlの内容を全てコピーした
- [ ] Supabaseにログインした
- [ ] SQL Editorを開いた
- [ ] + New query をクリックした
- [ ] SQLを貼り付けた
- [ ] RUNボタンを押した
- [ ] ✅ Success が表示された
- [ ] Table Editorで7つのテーブルを確認した

**全部チェックできたら完璧です！次は.env.localの設定に進みましょう！**
