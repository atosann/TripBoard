# 📝 SQLエディターでschema.sqlを実行する手順

## ステップ1: ファイルを開く

1. ダウンロードした `trip-board.zip` を解凍
2. `trip-board/supabase/schema.sql` ファイルを開く
3. **全ての内容をコピー** (Ctrl+A → Ctrl+C)

## ステップ2: Supabaseにログイン

1. https://supabase.com にアクセス
2. ログイン
3. プロジェクトを選択（作成していない場合は新規作成）

## ステップ3: SQLエディターを開く

```
Supabaseダッシュボード
↓
左メニューの "SQL Editor" をクリック
↓
画面右上の "+ New query" ボタンをクリック
```

## ステップ4: SQLを貼り付けて実行

1. 開いたエディター画面に、コピーした `schema.sql` の内容を **全て貼り付け**
2. 画面右下の **"Run"** ボタン（▶マーク）をクリック
3. 実行完了まで待つ（10-30秒程度）

## ステップ5: 実行結果を確認

### 成功の場合
✅ 緑色のチェックマーク ✓ が表示される
✅ 画面下部に "Success. No rows returned" と表示される

### エラーの場合
❌ 赤色のエラーメッセージが表示される
→ 以下を確認：
- SQLの内容が全てコピーできているか
- 既に同じSQLを実行していないか
- プロジェクトが正しく選択されているか

## ステップ6: テーブルが作成されたか確認

```
左メニュー "Table Editor" をクリック
↓
以下のテーブルが表示されれば成功！
- users
- posts
- categories
- participants
- messages
- reports
- activity_logs
```

## 📸 実際の画面イメージ

### SQL Editorの画面
```
┌───────────────────────────────────────────────────┐
│ SQL Editor                    + New query  ⚙️     │
├───────────────────────────────────────────────────┤
│                                                   │
│  -- Trip Board データベーススキーマ                 │
│  CREATE EXTENSION IF NOT EXISTS "uuid-ossp";      │
│                                                   │
│  CREATE TABLE public.users (                      │
│    id UUID PRIMARY KEY...                         │
│  );                                               │
│                                                   │
│  ... (schema.sqlの内容がここに表示される)           │
│                                                   │
│                                                   │
│                                      [▶ Run]      │
└───────────────────────────────────────────────────┘
```

### 実行成功時
```
┌───────────────────────────────────────────────────┐
│                                                   │
│  ✅ Success. No rows returned                     │
│  Execution time: 1.2s                             │
│                                                   │
└───────────────────────────────────────────────────┘
```

## 🔧 よくあるエラーと対処法

### エラー1: "relation already exists"
**原因**: 既に同じテーブルが存在する
**対処**: 
- 初めての実行なら無視してOK（一部のテーブルは既に存在する）
- または、Table Editorで既存テーブルを削除してから再実行

### エラー2: "permission denied"
**原因**: 権限がない
**対処**: 
- プロジェクトの所有者でログインしているか確認
- 別のプロジェクトを選んでいないか確認

### エラー3: "syntax error"
**原因**: SQLの一部が欠けている
**対処**: 
- schema.sqlの内容を**最初から最後まで全て**コピーし直す
- 改行や特殊文字が壊れていないか確認

## 💡 ヒント

- **全て選択**: Ctrl+A (Windows) / Cmd+A (Mac)
- **コピー**: Ctrl+C (Windows) / Cmd+C (Mac)
- **貼り付け**: Ctrl+V (Windows) / Cmd+V (Mac)

- SQLの実行は1回だけでOK！
- エラーが出ても、大抵は一部のテーブルだけ作成済みなだけで問題ない
- 実行後、必ずTable Editorでテーブルが作成されたか確認すること

## ✅ 完了確認

以下が全て確認できれば成功です：

- [ ] Table Editorで7つのテーブルが表示される
- [ ] categoriesテーブルにデータが入っている（初期カテゴリ）
- [ ] エラーメッセージが出ていない（または無視してOKなエラー）

成功したら、次は `.env.local` の設定に進みましょう！
